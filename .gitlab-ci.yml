image: python:2.7.12

flask-test:
  before_script:
    - "cd $CI_PROJECT_DIR"
    - "ls"
    - "pip install -r requirements.txt"
  script:
    - "python run.py ci"
  after_script:
    - bash <(curl -s https://codecov.io/bash)
